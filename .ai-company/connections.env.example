# Copy to apps/ai-company/.env.local (gitignored). Do NOT commit real values.

# Connections Bridge — trusted local runtime for secrets (AI-COMPANY-115)
CONNECTIONS_BRIDGE_HOST=127.0.0.1
CONNECTIONS_BRIDGE_PORT=17321

# Browser-safe only — no secrets here
VITE_CONNECTIONS_BRIDGE_HOST=127.0.0.1
VITE_CONNECTIONS_BRIDGE_PORT=17321

# Legacy DEV fallback remains available until Connections Center migration is complete.
