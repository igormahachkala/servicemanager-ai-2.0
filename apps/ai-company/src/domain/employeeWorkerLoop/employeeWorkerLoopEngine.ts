/**
 * Generic Employee Worker Loop — public API surface (AI-COMPANY-112G).
 * Implementation lives in maxWorkerLoopEngine (legacy module name).
 */

export {
  runEmployeeWorkerLoop,
  runMaxWorkerLoop,
  type RunEmployeeWorkerLoopParams,
} from '../maxWorkerLoop/maxWorkerLoopEngine'
