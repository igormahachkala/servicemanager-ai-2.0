# ServiceManager.AI Rules

- Full file replacement only
- Preserve multi-tenant isolation via companyId
- Never bypass PBAC/policy layer
- Business logic only in services
- No secrets in code
- NestJS + Prisma + PostgreSQL backend
- React + Vite frontend
- Prefer small modular files
- Always run build after frontend changes
