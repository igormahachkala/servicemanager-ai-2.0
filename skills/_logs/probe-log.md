# Журнал проб обнаружения скилов

Заведён 2 сентября 2026 по решению В-03. Сюда записывается результат шага 2
части E скила `sma-agent-setup`: сработал ли `sma-skill-probe` у конкретного
агента в конкретной среде.

Зачем в репозитории, а не в `_claude`: записи нужны другим агентам и должны
переживать переустановку машины. Каталог `_claude` в `.gitignore` и виден
только там, где создан.

Что писать в строке: дату, среду — агент, его версия, машина, — вершину
`origin/prod` на момент прогона и исход. Исход это либо «оба маркера
получены», либо какой именно не получен: `SKILL-PROBE-OK` — срабатывание,
`EXTERNAL-READ-OK` — чтение файла за пределами каталога скила.

Записи не удалять: отрицательный результат ценнее положительного, он
называет среду, в которой раскладка не работает.

| Дата | Среда | origin/prod | Исход |
|---|---|---|---|
| | | | |
