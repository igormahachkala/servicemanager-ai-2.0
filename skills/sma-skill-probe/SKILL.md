---
name: sma-skill-probe
description: Проверка того, что скилы этого репозитория подхватываются средой агента. Возвращает маркер срабатывания и маркер файла, лежащего вне каталога скила. Обязательно применять, когда пользователь просит проверить обнаружение скилов, срабатывание скилов, видит ли агент скилы, работает ли раскладка каталога скилов, а также при заведении новой машины или подключении нового агента. Также применять при запросах вида проверь механизм скилов, диагностика скилов, skill discovery, skill triggering. Шаг 2 части E скила sma-agent-setup вызывает эту проверку.
---

# Проверка обнаружения скилов

Скилл проверяет окружение: подхватывает ли среда агента скилы этого
репозитория и переходит ли агент по ссылке за пределы каталога скила.
Вызывается при заведении новой машины и при подключении нового агента,
шаг 2 части E скила sma-agent-setup (skills/sma-agent-setup/SKILL.md).
Изменений в репозитории не производит.

Автоматическая часть проверки — ссылки в .claude/skills и фронтматтер —
выполняется отдельно скриптом skills-check.sh, шаг 1 той же части. Здесь
проверяется то, что скриптом не проверяется: видит ли скилы сам агент.

## Что сделать

1. Сообщить маркер срабатывания: `SKILL-PROBE-OK`.

2. Сообщить полный путь файла, из которого прочитаны эти инструкции.
   Это отвечает на первый вопрос диагностики — из какого места скилл
   был подхвачен.

3. Прочитать файл `skills/_shared/probe-external.md` от корня репозитория
   и сообщить маркер, который в нём записан.

   Файл лежит вне каталога этого скила и попадает в репозиторий, поэтому
   проверка воспроизводится на любой машине. Если маркер из него прочитан
   и выведен, значит агент переходит по ссылке за пределы каталога скила —
   это второй вопрос диагностики. От ответа зависит, можно ли держать общий
   файл правил один раз и ссылаться на него из нескольких скилов, или его
   придётся копировать в каждый.

## Формат ответа

Три строки:

```
Маркер срабатывания: <маркер из пункта 1>
Прочитано из: <полный путь файла>
Внешний файл: <маркер из пункта 3, либо «не прочитан» с причиной>
```
